<%= form_with(model: intro, local: true) do |form| %>
  <% if intro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intro.errors.count, "error") %> prohibited this intro from being saved:</h2>

      <ul>
        <% intro.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :miku %>
    <%= form.text_field :miku %>
  </div>

  <div class="field">
    <%= form.label :hyogo %>
    <%= form.text_field :hyogo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
